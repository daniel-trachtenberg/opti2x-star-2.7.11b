# Opti2x STAR 2.7.11b

This repository contains an Opti2x-optimized **STAR 2.7.11b** source tree (best serial candidate).

**Result:** ~**1.60×** median on the frozen fixture. Status: **NOT_YET** (did not reach the 2× target). Micro-optimizations were exhausted; see the report for rejected avenues and evidence.

## Build

Build from `source/` per upstream STAR instructions (typically `make` in `source/`).

## Documentation

See `opti2x/OPTIMIZATION_REPORT.md`. Patch and checksums live under `opti2x/best_patch/`; evidence under `opti2x/evidence/`.
